version: 1.0
name: embalses

requirements:
  skip: false
  conda:
    channels:
      - conda-forge
    packages:
      - requests
      - pandas
      - google-api-python-client
      - xlrd
      - google-auth-oauthlib
